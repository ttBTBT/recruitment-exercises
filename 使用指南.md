# 招新做题仓库使用指南

仓库地址：https://github.com/ttBTBT/recruitment-exercises
本地路径：C:\Users\zbtbu\Documents\recruitment-exercises

## 一、分支结构

- `main`：主分支，只放说明文件，不放题目代码
- `question-01` ~ `question-11`：每道题一个分支，做题过程提交到对应分支

## 二、VSCode 图形化操作（推荐新手）

### 1. 切换分支
- 点 VSCode **左下角**的分支图标（写着 `main`）
- 在顶部弹出的列表里选 `question-01`（首次选择远程分支）
- 之后每做一道题切一次分支

### 2. 提交并上传（三步）
1. **暂存**：左侧源代码管理面板（Ctrl+Shift+G），点更改文件右边的 `+` 号
2. **提交**：在上方输入框写一句说明（如"完成第1题：排序"），按 Ctrl+Enter
3. **推送**：点面板里的 `...` → `推送(Push)`，或点左下角云朵图标

> 第一次推送时，浏览器会弹出 GitHub 登录授权页面，登录 `ttBTBT` 账号并点同意，以后就不用再登了。

## 三、终端命令操作（等价方式）

在 VSCode 里按 `` Ctrl+` `` 打开终端，依次输入：

```bash
git checkout question-01        # 切到第1题分支
# ... 写代码、做题 ...
git add .                       # 暂存所有改动
git commit -m "完成第1题"        # 提交
git push origin question-01     # 上传到 GitHub
```

做完一题，切下一题：

```bash
git checkout question-02
```

## 四、Kimi Code 插件使用

1. 左侧活动栏点 **Kimi 图标**（蓝色 K 字样）
2. 首次使用点 **登录**，浏览器打开授权页，登录 Kimi 账号后自动返回 VSCode
   （也可在聊天框输入 `/login`）
3. 在聊天框直接描述需求，例如：`帮我写一个判断回文数的 C 程序`
4. 用 `@文件名` 引用当前文件，Kimi 会读取代码上下文
5. Kimi 给出的修改会以 diff 形式展示，点 **接受** 才会写入文件

## 五、常见问题

- **推送失败 / 网络超时**：确认能正常打开 github.com；如打不开，先开代理软件再推送
- **改了代码没看到更改**：保存文件（Ctrl+S）后源代码管理面板才会刷新
- **提交错分支**：`git checkout 正确分支` 后重新提交即可
